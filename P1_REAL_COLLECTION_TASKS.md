# Quarzion P1：真实采集、证据与运营闭环（历史方案）

> 2026-08-27：本文件记录匿名网页版采集的历史实现，不再代表当前产品范围。当前正式任务以根目录 `TASKS.md` 和 `docs/ARCHITECTURE_API_MONITORING.md` 为准；腾讯元宝、网页自动化、账号池和多城市 IP 已移出本期。

更新时间：2026-08-21  
本轮范围：单区域、无登录网页版采集。  
明确延期：P1-05 账号/会话池、P1-06 多城市出口 IP。两项不计入本轮完成率，也不阻塞其他任务。

## 本轮目标

让豆包、腾讯元宝、DeepSeek 的真实网页回答进入 Quarzion，并且每一个客户指标都能回溯到原始回答、引用、截图和运行元数据。客户看到的全部是自己的真实数据；内部管理员可以调度、重试、排查和复核。

## P1-00 采集规则与数据标准

- [x] 固定平台代码与展示名称：`doubao/豆包`、`yuanbao/腾讯元宝`、`deepseek/DeepSeek`
- [x] 固定运行元数据：平台、区域、模型、采集器版本、开始/结束时间、耗时、任务尝试次数
- [x] 固定证据标准：原始回答、截图、引用 URL、页面地址、内容哈希、采集时间
- [x] 固定失败分类：网络、超时、页面改版、触发验证、平台阻断、空回答、解析失败、系统错误
- [x] 固定合规边界：低频、只读取公开页面、紧急停机、完整审计、禁止绕过验证码

验收：字段、状态、失败码和审计事件形成统一数据字典。

## P1-01 调度器与任务状态机

- [x] `runs` 作为唯一采集任务来源，停止维护重复任务数据
- [x] 原子领取与租约，防止多个采集器重复执行同一任务
- [x] 心跳、超时回收、失败退避和最大重试次数
- [x] 幂等回传，同一运行重复回传不会产生重复回答或引用
- [x] 平台级暂停、恢复、并发和超时策略
- [x] 全局紧急停机开关
- [x] 运行尝试历史完整保留

验收：并发领取不重复；掉线任务可回收；重试不重复写证据。

## P1-02 豆包无登录网页采集器

- [ ] 打开豆包公开网页并提交提示词
- [ ] 等待回答真正结束，不采集中间流式文本
- [ ] 保存完整回答、引用、截图和页面元数据
- [x] 识别登录墙、验证码、阻断、超时和页面结构变化
- [x] 安全失败并进入异常中心，不尝试绕过验证

2026-08-21 上海校准：公开首页可访问，输入动作可完成，但匿名提交后跳回 `?from_logout=1` 且问题没有执行；采集器已准确记录 `login_required`，并返回截图、HTML 和页面指纹。

验收：使用真实公开页面完成至少 10 条连续运行，证据可回溯。

## P1-03 腾讯元宝无登录网页采集器

- [ ] 打开腾讯元宝公开网页并提交提示词
- [ ] 等待回答真正结束
- [ ] 保存完整回答、引用、截图和页面元数据
- [x] 识别登录墙、验证码、阻断、超时和页面结构变化
- [x] 安全失败并进入异常中心

2026-08-21 上海校准：匿名页面可打开，但输入框明确显示“请登录后输入内容”；采集器已准确记录 `login_required`，不会尝试提交或绕过登录。

验收：使用真实公开页面完成至少 10 条连续运行，证据可回溯。

## P1-04 DeepSeek 无登录网页采集器

- [ ] 打开 DeepSeek 公开网页并提交提示词
- [ ] 等待回答真正结束
- [ ] 保存完整回答、引用、截图和页面元数据
- [x] 识别登录墙、验证码、阻断、超时和页面结构变化
- [x] 安全失败并进入异常中心

2026-08-21 上海校准：地域 403 已消失，但网页直接跳转 `/sign_in`；采集器已准确记录 `login_required`，并返回截图、HTML 和页面指纹。

验收：使用真实公开页面完成至少 10 条连续运行，证据可回溯。

## P1-05 账号与会话池（延期）

- [DEFERRED] 豆包、腾讯元宝、DeepSeek 账号池
- [DEFERRED] 账号健康度、冷却、并发、预算和自动切换
- [DEFERRED] 登录会话加密与失效处理

当前仅保留 `anonymous` 执行接口，不启用现有账号池页面和数据。

## P1-06 多城市出口 IP（延期）

- [DEFERRED] 城市出口节点
- [DEFERRED] IP 健康度与成本管理
- [DEFERRED] 同提示词跨城市对比

当前区域固定为单一区域；运行数据仍保留 `region` 字段，后续无需重构证据链。

## P1-07 解析器与真实指标

- [x] 品牌和已审核别名提取
- [x] 品牌首次出现位置、提及次数、提及率和响应份额
- [x] 引用 URL、域名、标题和来源类型
- [x] 情绪只作为透明的规则分数，不伪装成确定事实
- [x] 解析器版本留档，支持未来重新计算
- [ ] 人工复核错误实体和别名

验收：每一个聚合指标都能展开到组成它的真实回答。

## P1-08 原始证据库

- [x] 保存回答原文、完整页面截图、引用和元数据
- [x] 对回答和截图生成 SHA-256，证明内容未被悄悄修改
- [x] 证据按组织和项目隔离
- [x] 客户只能读取自己项目的证据
- [x] 管理员可以查看失败尝试和诊断信息
- [x] 限制文件大小与内容类型，防止任意文件写入

验收：证据从客户指标一路可追溯；跨组织访问返回拒绝。

## P1-09 客户后台真实数据

- [x] 概览、平台表现、趋势和品牌榜单只使用真实数据库
- [x] 新增证据库与回答详情
- [x] 展示平台、区域、模型、采集器版本、采集时间和证据哈希
- [x] 展示新鲜度、覆盖率、成功率和失败数
- [x] 无数据时明确显示空状态，不生成示例趋势
- [x] 所有图表提供可读表格或明细入口

验收：客户页面无模拟指标；所有明细受组织和项目权限保护。

## P1-10 管理员运营台

- [x] 使用 `runs` 展示真实任务队列
- [x] 查看采集器在线状态、租约、耗时、成功率和失败原因
- [x] 平台暂停/恢复、任务重试和紧急停机
- [x] 证据抽检和原始回答查看
- [ ] 查看页面结构变化与采集器版本分布
- [x] 将账号池和地域 IP 区域标记为“延期、未启用”

验收：运营人员不操作数据库即可排查、重试和暂停采集。

## P1-11 监控与异常中心

- [x] 内部异常中心：验证码、阻断、改版、超时、空回答、解析失败
- [x] 相同异常聚合去重，保留首次和最近发生时间
- [x] 可确认、解决、重新打开
- [x] 平台成功率下降、队列积压和采集器离线告警
- [x] 内部健康检查与日志留档
- [DEFERRED] 外部邮件通知随邮箱配置一起后置

验收：异常可被发现、定位到运行、处理并审计。

## P1-12 安全、合规与成本保护

- [x] 采集器密钥无停机轮换与租约校验
- [x] 回传大小限制、URL 白名单和输入清洗
- [x] 客户证据下载权限校验
- [x] 敏感字段不进入日志和前端响应
- [x] 平台级速率限制、并发上限和每日预算
- [x] 紧急停机与恢复审计
- [x] 明确不绕过验证码、不对抗平台安全机制

验收：越权、重放、超大回传和非法 URL 测试均被拒绝。

## 实施顺序

1. 数据迁移、任务状态机和异常中心接口
2. 三平台采集器框架与安全失败分类
3. 真实平台适配和证据入库
4. 解析器、客户证据库和真实指标
5. 管理员运营台和紧急停机
6. 自动化安全/隔离/幂等测试
7. 生产迁移、备份、部署和回滚验证
8. 启动 7 天稳定性观察

## 本轮发布闸门

- [x] 豆包、腾讯元宝、DeepSeek 均有真实回答样本或明确的公开页面阻断证据
- [ ] 成功回答均包含原文；可获得引用时保存引用；可截图时保存截图
- [x] 客户指标 100% 来自真实回答，不使用模拟数据
- [x] 跨组织和跨项目证据访问被拒绝
- [x] 任务领取、租约回收、退避重试和幂等回传测试通过
- [x] 失败原因可识别、可重试、可审计
- [x] 数据库备份和恢复验证通过
- [ ] 生产发布后开始 7 天稳定性观察，目标成功率不低于 90%

说明：7 天观察未结束前，状态只能是“P1 已上线观察”，不能宣称“P1 稳定性验收完成”。
