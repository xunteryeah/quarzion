# Quarzion P0 正式化任务清单

最后更新：2026-08-20（Asia/Shanghai）  
当前阶段：`P0-09` 已正式上线并完成生产验收；按 2026-08-20 决定保留 `queue_only`，邮件与外部邮件告警真实投递明确延期  
目标：真实客户进入前，完成身份、租户隔离、统一数据、运维安全和正式域名闭环。

## 不可妥协的验收规则

- 任何客户都不能读取或修改其他组织的数据。
- 所有客户接口必须在服务端校验用户、组织、项目和操作权限。
- 真实环境不得自动写入演示客户、演示回答或演示趋势。
- 管理端创建的客户、项目和用户必须直接出现在同一生产数据源中。
- 每项生产变更都必须具备备份、测试、部署验证和回滚路径。
- P0 验收通过前，不把演示数据当成真实监测结果交付客户。
- P1 真实采集器、号池和地区 IP 不提前混入 P0，避免扩大故障面。

## P0-00：冻结现状、备份与验收基线

- [x] 正式域名 `quarzion.com` 上线。
- [x] 管理后台迁移到 `admin.quarzion.com`。
- [x] HTTPS、旧域名跳转和服务器健康检查完成。
- [x] 保存前台、管理端、Nginx、环境配置和数据库卷的完整快照。
- [x] 导出当前前台与管理端数据库结构及数据量清单。
- [x] 标记所有演示客户、演示项目、固定日期和模拟指标来源。
- [x] 建立 P0 验收测试矩阵：登录、权限、隔离、备份、限流和联系表单。
- [x] 编写并验证整站回滚步骤。

## P0-01：统一生产数据库

- [x] 确定 P0 采用统一 SQLite/D1 兼容数据库，并明确 PostgreSQL 扩容边界。
- [x] 设计统一数据模型：用户、组织、组织成员、项目、品牌、提示词、运行、回答、引用和审计。
- [x] 将管理员后台的客户与项目模型合并到统一数据库。
- [x] 将客户后台的项目、监测和证据模型迁入统一数据库。
- [x] 建立正式数据库迁移脚本，禁止生产启动时自动写入 Seed 数据。
- [x] 将前台和管理端连接到同一生产数据源；两个容器均挂载 `quarzion_unified-data:/data`。
- [x] 验证管理端创建项目后，客户后台无需复制即可读取同一项目。
- [x] 完成旧 SQLite 数据的可回滚归档，不直接丢弃原数据。

环境边界：本地开发库、独立演示库、线上正式库三者不得共用数据库文件或客户记录。

## P0-02：客户登录、组织和邀请

- [x] 实现客户登录、退出和安全会话 Cookie。
- [x] 增加用户、组织、组织成员和邀请记录。
- [x] 管理员可以创建客户公司、客户项目和首位客户管理员。
- [x] 管理员可以生成邀请、撤销邀请和重新发送邀请；旧链接立即失效，回归测试通过（实际邮件投递随正式邮件通道启用）。
- [x] 客户接受邀请后只能进入所属组织空间。
- [x] 实现基础客户角色：组织管理员、成员、只读访客。
- [x] 增加登录失败限制、会话失效和退出全部设备能力。
- [x] 密码或登录凭据不得以明文保存在数据库、日志或前端。

## P0-03：项目隔离与全接口权限校验

- [x] 建立统一服务端授权函数和权限矩阵。
- [x] 所有读取接口校验当前用户所属组织和项目。
- [x] 所有新增、修改、删除和重试接口校验操作权限。
- [x] 禁止通过修改 URL、项目 ID 或请求参数访问其他客户数据。
- [x] 管理端权限与客户权限彻底分离。
- [x] 为越权、未登录、过期会话和跨组织访问编写自动化测试。
- [x] 在审计日志中记录登录、邀请、权限变更和敏感操作。

## P0-04：演示与生产数据隔离

- [x] 演示/历史数据卷与生产统一数据卷完全分开；旧数据卷保留但未挂载到正式容器。
- [x] 生产数据库移除腕表、汽车等预置演示客户和回答；生产清单验证组织、项目、用户、运行、回答均为 `0`。
- [x] 删除客户后台中的模拟趋势、固定日期和固定统计结果。
- [x] 空项目展示真实空状态和接入指引，不伪造增长数据。
- [x] 平台枚举统一为豆包、腾讯元宝、DeepSeek，并为后续平台保留扩展能力。
- [x] 历史数据明确标注平台、地区、模型版本、采集版本和时间。
- [x] 管理后台与客户后台使用同一套平台名称和状态定义。

## P0-05：正式域名结构

- [x] 配置 `app.quarzion.com` 作为客户登录和客户后台。
- [x] 配置 `api.quarzion.com` 作为程序接口；健康接口返回 `200`，根路径返回 `404`。
- [x] 保持 `quarzion.com` 为品牌官网。
- [x] 保持 `admin.quarzion.com` 为内部管理后台；公网无凭据返回 `401`。
- [x] 为新子域名配置 DNS、HTTPS、安全响应头和不索引策略。
- [x] 将现有 `/dashboard` 平滑跳转到 `app.quarzion.com`，保留过渡期兼容。
- [x] 确认采集器接口只对授权 Worker 开放；无密钥的领取和回传请求均返回 `401`。

## P0-06：备份、恢复、日志、通知与限流

- [x] 每日自动备份生产数据库，并设置 14 天本地保留周期。
- [x] 每日备份加密同步到独立 Mac，密钥只保存在 Mac；已完成解密、SHA-256 与 SQLite 完整性演练。
- [x] 定期备份应用配置、Nginx 配置和必要密钥引用。
- [x] 编写一键恢复流程，并在非生产环境完成一次恢复演练。
- [x] 增加结构化访问日志、应用错误日志和审计日志。
- [x] 对登录、联系表单、客户接口和管理接口设置独立限流策略。
- [ ] 对连续登录失败、接口错误、数据库异常和备份失败发送外部通知；本地告警日志与健康任务已启用，外部邮件通知随 Resend 一并延期。
- [x] 建立健康检查、磁盘容量、容器状态和证书到期监控。
- [x] 日志中自动遮蔽密码、会话、Token 和其他敏感信息。

## P0-07：联系表单与整体验收


- [x] 联系表单执行服务端校验和反垃圾处理。
- [x] 表单提交写入统一数据库中的内部“咨询线索”CRM；邮件通道接通后同时发送到公司邮箱。
- [x] 保存提交状态、失败原因和必要审计记录，不泄露客户隐私。
- [x] 给用户提供明确的成功、失败和重试反馈。
- [x] 完成桌面端和移动端的登录、客户后台和联系表单测试。
- [x] 完成跨组织数据隔离渗透测试。
- [x] 完成数据库备份恢复演练和整站失败自动回滚演练。
- [x] 验证生产环境不存在演示数据、固定趋势或无权限接口。
- [x] 输出 P0 上线报告和遗留风险清单。
- [ ] P0 全部验收通过后，才能开始接入首个真实客户。

## 当前外部前置条件

- [x] 在 Spaceship 增加 `app` → `47.236.194.150` 的 A 记录，并从 Cloudflare、Google 与阿里公共 DNS 验证。
- [x] 在 Spaceship 增加 `api` → `47.236.194.150` 的 A 记录，并从 Cloudflare、Google 与阿里公共 DNS 验证。
- [x] 扩展 `quarzion.com` 正式证书，使其覆盖官网、`www`、`admin`、`app` 与 `api`，并保留自动续期。
- [ ] 配置 `RESEND_API_KEY`，并验证 `QUARZION_FROM_EMAIL` 发信域名，供联系表单和客户邀请发送邮件。
- [x] 配置显式 `queue_only` 模式：咨询与邀请加密入库、不会消耗重试次数，管理员可复制邀请链接；不得伪装成邮件已送达。
- [x] 异常告警支持同一 Resend 通道发送到运营邮箱；Webhook 保留为可选的第二通道。
- [x] Mac 加密异地备份任务已安装，每天 04:20 自动拉取，并向服务器写入新鲜度回执。

## 当前生产状态

- [x] 正式版本 `20260820-p0-09` 已上线；运行镜像为 `quarzion-frontend:p0-20260820-09`、`quarzion-admin:p0-20260820-09`，两个容器均为 `healthy`。
- [x] 候选环境已验证登录、组织/项目共库、邀请接受、联系表单、两版迁移和内部接口隐藏。
- [x] 咨询与邀请邮件已使用加密持久队列，支持自动重试、卡住恢复、终止失败和撤销取消。
- [x] 生产随机密钥已生成并保留切换前备份；密钥值未写入代码、日志或任务文档。
- [x] 发布前检查、证书扩展、备份、发布、健康验证和失败自动回滚脚本已固化。
- [x] Nginx 候选配置语法通过，邀请 Token 会在访问日志中自动遮蔽。
- [x] `queue_only` 模式下发布门禁明确跳过真实邮件并记录延期事件；切换为 `resend` 时必须先通过邮件与告警真实投递验证。管理容器按最小权限隔离无关密钥。
- [x] 官网咨询会进入内部“咨询线索”页面，邮件失败时仍可查看，并支持已联系/已关闭状态和审计记录。
- [x] 当前生产环境已启用每日自动备份和每 5 分钟健康检查；首次正式备份已恢复验证通过，未改动线上网页与路由。
- [x] 当前生产 Nginx 已启用结构化 JSON 访问日志、14 日轮转、安全响应头和独立限流；实测邀请 Token 被遮蔽且登录突发请求返回 `429`。
- [x] 当前前置检查会准确阻止缺少 DNS、核心密钥、镜像或有效异地备份回执的上线；`resend` 模式额外强制检查邮件与告警通道。
- [x] 首次正式切换演练因 Nginx 未重新加载而触发自动回滚；旧官网与旧数据卷恢复成功。已修正部署/回滚两侧的 Nginx reload、定时任务状态恢复和空 `current` 指针处理。
- [x] 正式生产创建两家临时公司并实测：跨组织读取隔离、跨组织写入拒绝、邀请撤销/重发、退出失效、咨询入库与加密均通过；临时记录随后精确清零。
- [x] 修复 Vinext 字体资源 `404` 与构建机本地路径泄露；正式页面响应不再包含 `/Users/...` 或失效字体地址。

## P0 之后的路线

- P1：豆包、元宝、DeepSeek 真实采集器、账号池、调度器、失败重试和原始证据。
- P2：优化任务、前后基线、客户周报月报、PDF/Excel 和可分享证据。
- P3：套餐额度、合同续费、员工角色、Webhook、企业微信和多服务器扩容。
