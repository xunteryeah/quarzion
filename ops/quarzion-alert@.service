[Unit]
Description=Send a WindCall systemd failure alert for %i

[Service]
Type=oneshot
EnvironmentFile=-/opt/quarzion/.env
ExecStart=/opt/quarzion/scripts/notify-alert.sh "WindCall systemd unit failed: %i"
User=root
Group=root
